# Dependencies

Use the full locked runtime in ../INSTALL.md. recipes/recipe_manifest.csv is the backend inventory. scripts/check-environment.R reports actual package versions, font/interpreter paths and missing capabilities.

Core-only developer runs report specialized skips. A skipped formal check is not success. Plotting never installs packages or substitutes another chart family.
